# SSAIREN AI ↔ Backend 연동 설계 (데모) — 2026-06-13 17:57

## 1. 전체 흐름 (확정)

```
FE ──①POST /ai/use/trigger──▶ Spring ──②POST /api/v1/pipeline-a/runs/demo──▶ FastAPI(기존 API)
                                                                                  │ 202 즉시응답
                                                                                  │ 백그라운드 데모 에이전트 실행
                                                                                  │
  Spring tool 엔드포인트 ◀──③POST tool 콜백들 (FCM/증거/경찰/GPS)────────────────┤  ← FastAPI 툴콜링
                                                                                  │
  Spring /ai/use/callback ◀──④POST 종합 결과 push (no-op, 주석만)────────────────┘  ← FastAPI 콜백
```

원칙: **FastAPI 는 "툴콜링" 중심.** GET/폴링 안 함. Spring 이 다 처리.
트리거: `FE → Spring → FastAPI(기존 /runs/demo)` 로 확정.

## 2. Backend 엔드포인트 (`domain/ai/use`)

### 오케스트레이션 2개
| 엔드포인트 | 방향 | 역할 |
|-----------|------|------|
| `POST /ai/use/trigger` | FE → Spring | 데모 시작. Spring 이 FastAPI `/runs/demo` 대신 호출. **유지** |
| `POST /ai/use/callback` | FastAPI → Spring | 종합 결과 push 수신. **유지하되 지금은 no-op(수신만, 내부 주석)** |

### 데모 tool 콜백 5개 (FastAPI tool → Spring, 전부 POST)
| # | 엔드포인트 | AI tool |
|---|-----------|---------|
| 1 | `POST /ai/use/notifications/family-alert` | send_family_sms_alert |
| 2 | `POST /ai/use/notifications/warning-banner` | show_warning_banner (FCM 분리) |
| 3 | `POST /ai/use/evidence` | save_evidence |
| 4 | `POST /ai/use/police/report` | notify_police |
| 5 | `POST /ai/use/family/gps` | check_family_gps (연락처 조회·GPS는 Spring 내부 처리) |

> `GET /ai/use/family/contacts` 는 **드롭** (FastAPI 는 GET 안 함). check_family_gps 는 POST 하나로 단순화.

tool 엔드포인트는 우선 **요청 수신·검증·로그·성공응답(데모급)**, 실제 FCM/GPS 연동은 확장 지점으로 주석 표시.

## 3. Backend 패키지 구조 (`domain/ai/use`)

```
controller/  AiUseController(trigger, callback), AiToolCallbackController(tool 5개)
service/     AiUseService(트리거 호출, 콜백 no-op), AiToolService(tool no-op 처리)
client/      AiDemoClient (RestClient → FastAPI /runs/demo)
dto/         AiTriggerRequest/Response, AiDemoRunRequest/Response,
             AiAnalysisCallbackRequest, ToolActionResponse,
             FamilyAlertRequest, WarningBannerRequest, EvidenceRequest,
             PoliceReportRequest, GpsRequest
```

설정(application.yaml 추가 예정):
```
ssairen.ai.use.fastapi-url   (기본 http://localhost:8000)
ssairen.ai.use.demo-path     (기본 /api/v1/pipeline-a/runs/demo)
```

## 4. AI(FastAPI) 측 수정 (`ai2spring` 브랜치)

1. `tools/actions.py` — 데모 5개 tool 경로를 `/ai/use/...` 로 변경, **POST 만**, FCM 2개 분리,
   check_family_gps 를 POST `/ai/use/family/gps` 하나로 단순화(GET 제거).
2. `api/v1/pipeline_a.py` — 데모 작업이 tool 을 실제 실행하도록 `execute_tools=True` 허용
   (단, `SPRING_API_URL` 미설정 시 tools/client 가 DRY_RUN 으로 안전 처리).
3. 데모 작업 완료 후 `SPRING_API_URL + /ai/use/callback` 으로 종합 결과 POST(push) 추가.
4. `core/config.py` — 콜백 경로 설정 추가(`/ai/use/callback`), 기존 `SPRING_API_URL`/`SPRING_INTERNAL_API_KEY` 재사용.

## 5. tool 호출 바디 (AI → Spring, camelCase)

- family-alert: `{ callId, userId, scenario, situationSummary, riskScore, guardianIds[] }`
- warning-banner: `{ callId, userId, scenario, riskScore, situationSummary, message }`
- evidence: `{ callId, userId, conversationText, riskScore, scenario }`
- police/report: `{ callId, userId, riskScore(int), incidentType, summary }`
- family/gps: `{ callId, userId }`

콜백 바디(AI → Spring): `{ callId, detectedScenario, riskLevel, riskScore, detectedKeywords[], situationSummary, toolsToCall[], finalActionsTaken[], response }`

## 6. 진행 상태 (2026-06-13 17:57 기준)

작업 브랜치:
- AI: `ai2spring` (origin/main 기준)
- Backend: `feat/ai_spring` (origin 기준)

Backend `domain/ai/use` 생성 완료한 파일:
- dto: AiTriggerRequest, AiTriggerResponse, AiDemoRunRequest, AiDemoRunResponse,
  AiAnalysisCallbackRequest, ToolActionResponse, FamilyAlertRequest, WarningBannerRequest

남은 작업:
- dto: EvidenceRequest, PoliceReportRequest, GpsRequest
- client: AiDemoClient
- service: AiUseService, AiToolService
- controller: AiUseController, AiToolCallbackController
- application.yaml 설정 추가
- AI 측 수정(위 4번 항목) + 컴파일/구동 검증
